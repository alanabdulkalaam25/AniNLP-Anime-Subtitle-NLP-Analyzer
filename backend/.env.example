# Comma-separated frontend URLs permitted to call the API.
# For a Vercel deployment, replace the second value with your Vercel URL.
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
# Public analysis protection defaults: 10 requests per client per 60 seconds.
# Resource limits are configured in app/core/config.py.
